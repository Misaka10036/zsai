# Python 后端业务门户修复与验证（2026-09-21）

范围：智盛fontend、门户回归测试和 Windows 本地 PHP 启动方式。现有 Python 后端接口保持不变。

## 已修复

| 原问题 | 当前行为 |
|---|---|
| 图谱提交失败误报成功、读取失败误报空图谱 | 检查 HTTP、业务 code 和任务 ID；明确显示错误，提交不等同于完成。读取采用请求版本校验，切换知识库不再写入旧结果；空图谱释放旧图表实例以便刷新。 |
| 普通用户看到管理员操作 | 普通用户不显示图谱构建、知识库新建、文档编辑及停止解析入口；PHP 继续校验管理员角色。 |
| 禁用/删除用户后已有登录仍有效 | 每次受保护页面及 API 请求重新读取用户状态和角色，失效账号清除登录。已经开始执行的请求不追溯撤销。 |
| 知识库、聊天、搜索列表只取前 50 条 | 统一列表读取器按页加载全部数据，文件关联知识库和用户管理也使用该读取器；失败明确显示错误。 |
| 文档列表失败显示为无文档 | 检查业务 code，显示错误；丢弃旧知识库的延迟响应。 |
| 上传部分失败无法区分、重试重复上传 | 逐文件显示结果，仅保留失败文件供重试；上传期间禁止重复提交及关闭弹窗。 |
| 上传后缺少解析衔接 | 默认勾选上传后解析，仅提交成功文档 ID；解析提交失败明确说明文件已上传，并提示在列表重试解析。可取消自动解析。运行中/排队任务每 5 秒刷新状态，显示后端进度说明。 |
| 新建搜索应用未绑定知识库 | 创建时必须选择知识库；管理员可以编辑现有应用的名称、描述和知识库。 |
| 搜索应用缺少对话模型 | 创建和编辑必须选择对话模型，写入 `search_config.chat_id`。已有应用在检索或思维导图时若缺少该字段，用租户默认对话模型补齐，且不会覆盖刚刚保存的模型。模型鉴权失败时提示检查模型配置。 |
| 搜索请求覆盖新应用、重复提交 | 请求版本与应用关联，切换后丢弃旧响应，同一检索期间禁止重复提交。 |
| PDF 预览依赖未满足、内网资源不可达 | 搜索页统一加载本地 PDF.js、worker、CMap、标准字体和 ECharts；所有页面的 Bootstrap、图标和 ECharts 改为本地资源。管理员原来加载公网 PDF.js，普通用户页原来缺失，现已统一。 |
| Word/Excel 等原文件被当作文本 | 在线预览限于 PDF 和支持的文本格式；其余格式明确提示下载后打开，不显示二进制乱码。 |
| 搜索思维导图无法显示 | 保留搜索流里的 mindmap；未返回时可点击生成，使用普通 API Key 可访问的 Python `/chat/mindmap`，按搜索应用知识库生成，失败可重试。 |
| 聊天新会话残留旧消息、历史读取错误被吞 | 新建会话立即切换并加载该会话；历史读取失败明确提示。 |
| 聊天响应跨会话、重复发送 | 请求与应用、会话及视图版本绑定；同一会话只允许一个回答请求；失败保留问题供重试。 |
| 聊天只显示正文 | 当前回答和历史消息展示引用片段及原文链接。 |
| 聊天与 Agent 等待完整 JSON 才显示 | PHP 实时透传 Python SSE；聊天按文本增量更新、空 final 只更新引用，非空 final 替换正文；Agent 按 message 增量更新并展示节点事件。思考内容折叠显示，保持 Markdown 安全渲染。 |
| 流式中断与结束状态 | 识别聊天 data=true 和 Agent [DONE]；缺少结束标记、HTTP/业务错误明确报错，保留已收到的正文。Agent 取消仍调用后端任务接口并等待请求结束，不因断开浏览器连接而误报已取消。 |
| Agent 假数据/默认流程掩盖错误 | 删除模拟列表、默认流程；列表/详情失败显示真实错误并禁用运行。移除无实现的展开/收起入口。 |
| Agent 停止只改页面、会话不延续 | 先创建真实会话；后续运行带 session_id；停止调用后端取消接口并等待原请求结束，取消失败不提示成功。取消后新建会话，正常连续运行复用会话。 |
| PHP 会话锁和单线程开发服务器阻塞取消 | 长请求前释放 PHP Session 锁。本地启动脚本启动两个普通 PHP worker 和一个专用取消 worker，由同源 Node 代理分发；Ctrl+C 统一清理子进程。 |
| 占位入口和重复实现 | 删除未使用的假解析函数、重复文档列表/分页实现、页脚占位链接和普通用户首页假 Agent 入口。 |
| 本机 PHP 直连地址仍为 Vite 端口 | 本机被 Git 忽略的 config.local.php 已改为 `http://127.0.0.1:9380`；密钥和数据库配置保留。启动脚本仍可用 `-BackendUrl` 覆盖。 |

## 自动验证

- PHP 契约测试 28 项通过，覆盖 Python 路由/方法、上传、解析、搜索、聊天、图谱、Agent 会话与取消、登录状态刷新、思维导图和错误传播；新增测试在后端尚未结束时检查 PHP 首事件已到达，并验证流式 HTTP/业务错误。
- 图谱与搜索页面原回归通过：图谱提交 8 种结果、读取 4 种失败、两类用户搜索选择/展示/HTML 清理。
- 工作流回归 9 组通过：分页和角色、聊天隔离/新建/失败恢复/引用、搜索配置与请求隔离、上传部分失败重试与解析、文档错误/只读操作/二进制预览、Agent 取消/失败/会话延续、本地资源完整性、真实 PDF 字节解析、独立取消代理。
- 流式页面回归覆盖逐字节 UTF-8/CRLF 分包、结束前可见正文、思考事件、最终引用、JSON 鉴权失败、格式错误、业务失败、连接中断，以及 Agent 消息和节点事件。
- PHP/JavaScript 语法、PowerShell 启动脚本语法及 Git 差异空白检查通过。

运行：

```sh
python test/unit_test/vivarly/test_portal_contract.py
node test/unit_test/vivarly/portal-ui.test.cjs
node test/unit_test/vivarly/portal-workflows.test.cjs
node test/unit_test/vivarly/portal-streaming.test.cjs
```

契约和页面测试使用本机模拟响应；PDF 测试使用真实本地解析器，代理测试使用本地 HTTP 服务。不修改真实知识库。

## 仍需真实服务联调

本次检查时 Python API、门户 MySQL 未连通。模型、解析 worker、存储和索引服务的实际执行结果尚未验证。聊天与 Agent 的流式链路已通过模拟事件验证；真实模型输出、历史持久化及部署代理是否关闭响应缓冲仍需联调。搜索仍收集完整回答后显示；普通聊天尚无停止生成按钮。独立门户账号、共享 API Key 租户资源保持现有设计。Agent 的 user_inputs 事件会提示所需输入；结构化表单提交不在本轮流式功能范围内。

1. 启动 Python API、门户 MySQL、索引/存储依赖、解析 worker，并配置有效 API Key、默认模型及聊天助手。
2. 使用 `start_frontend.ps1` 启动门户。生产环境使用支持并发请求的 PHP-FPM/Web 服务；直接 `php -S` 的单进程模式无法及时处理长请求期间的取消。
3. 在专用测试知识库上传文本/PDF，确认逐项结果、解析完成、失败原因和仅失败项重试。
4. 在门户创建/编辑搜索应用，绑定该知识库并选择对话模型；测试回答、引用 PDF 定位、思维导图、聊天会话切换和历史引用。若租户默认对话模型鉴权失败，在应用设置中改选可用模型，或先修复该 provider 的 API Key。
5. 测试图谱任务完成后刷新、普通用户只读限制、禁用账号后下一请求退出，以及 Agent 连续会话和后台取消。
6. 仅清理本次创建的测试资源。
